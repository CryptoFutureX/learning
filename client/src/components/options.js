const options = {
    candlestick: {
        wick: {
            useFillColor: true,
        },
    },
    chart: {
        toolbar: {
            show: false,
        },
    },
};

export default options;
